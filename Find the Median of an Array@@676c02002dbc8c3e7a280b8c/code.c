#include<stdio.h>
int main()
{
    int i,j,sum=0,median;
    int size = sizeof(num) / sizeof(arr[0]);
    scanf("%d",&i);
    int num[i]
    for(j=0;j<i;j++)
    {
        scanf("%d",&num[j]);
    }
    for(j=0;j<i;j++)
    {
        sum=sum+num[j];
    }
    median=sum/size;
    printf("%d",median);
}