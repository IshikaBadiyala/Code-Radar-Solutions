#include<stdio.h>
int main()
{
    int i,j,sum=0,median;
    int size = sizeof(arr) / sizeof(arr[0]);
    scanf("%d",&i);
    int arr[i]
    for(j=0;j<i;j++)
    {
        scanf("%d",&arr[j]);
    }
    for(j=0;j<i;j++)
    {
        sum=sum+arr[j];
    }
    median=sum/size;
    printf("%d",median);
}