#include<stdio.h>
#include<string.h>

 int main()
    {
        char str[30];
        scanf("%s",str);
       int count=strlen(str);
        for(int i=0;i<=count;i++)
        {
            printf("%s",str);
        }
    }

